# Prod SSR patch — 2026-09-08

Trạng thái: **đã deploy, technical smoke PASS; chờ Hưng xác nhận UAT trên thiết bị của mình**. GO: “bạn deploy lên Prod rồi guide mình Prod test simple trước và sau nhé ?”.

## Bản được phát hành

- Base `main@c36f42e008c303dd339d90439510b89054dfc05f` cộng bản vá local chưa commit/push.
- Deployment mới: `dpl_EH7USNZ7YgZmuFD2ef3EraX8bHXv`.
- URL: https://hungreo-website-hojo1twmk-hungreos-projects.vercel.app
- Patch SHA256: `0d112e5c4b02072e2f7aa72f8de4fb6812d1c59665a7e0acc71e196a94df482b` (path + NUL + bytes cho từng file theo thứ tự manifest).
- Deployment trước: `dpl_E2GCmbYJgPq9EEn3sCQVDbdVQGyA`, giữ làm mốc rollback; chưa thực hiện rollback.
- Nguồn deploy tạo từ `git archive HEAD` và chỉ chép 6 file patch/test. Không đưa local env hoặc các tài liệu dirty vào artifact deploy.
- Thay đổi: hai import DOMPurify ở `app/blog/[slug]/page.tsx`, `app/projects/[slug]/page.tsx`; dependency/package scripts ở `package.json`, `package-lock.json`; regression test `__tests__/detailPagesSSR.test.ts`, fixture helper `scripts/serve-ssr-fixtures.ts`.
- Không đổi DNS, redirect, CMS, credentials, dữ liệu hoặc chatbot. `vercel alias ls` xác nhận hungreo.com, www.hungreo.com và hungreo.vercel.app trỏ bản mới; cả ba vẫn phục vụ trực tiếp như trước.

## Kiểm tra thực tế

| Thời điểm (VN, 08/09/2026) | Kết quả |
|---|---|
| 18:58–18:59, trước deploy | GET 6 trang published: 6/6 HTTP 500 |
| Sau build, trước promote | `vercel curl` tới candidate: 6/6 HTTP 200, đúng title, không có fallback 500 |
| 19:05, sau promote | GET 6 trang × 2 lượt: 12/12 HTTP 200, đúng title, không có fallback 500 |
| Sau promote, Chrome | 6 trang × EN/VI: 12/12 có tiêu đề đúng ngôn ngữ và nội dung article |
| Sau promote, public routes | Home/About/Projects/Blog/AI Tools: HTTP 200 |
| Sau promote, anonymous auth | Dashboard chuyển tới login; session null; provider callback dùng hungreo.com |
| 19:01:40–19:06:36, logs deployment mới | Hai truy vấn riêng `5xx` và `ERR_REQUIRE_ESM`: không có log phù hợp |

Bằng chứng request + manifest: [PROD_SSR_2026-09-08_EVIDENCE.json](PROD_SSR_2026-09-08_EVIDENCE.json). Browser kiểm tra trong session Chrome hiện có; anonymous auth kiểm tra riêng bằng HTTP không cookie. Chưa test đăng nhập hoặc thao tác ghi admin. Không chạy payload XSS trên Prod; đã kiểm tra sanitizer bằng fixture local trước deploy.

Commands chính:

```bash
vercel deploy --prod --skip-domain --yes --meta baseCommit=c36f42e008c303dd339d90439510b89054dfc05f --meta ssrPatchSha256=0d112e5c4b02072e2f7aa72f8de4fb6812d1c59665a7e0acc71e196a94df482b
vercel curl /projects/openclaw-personal-ai-operations-platform --deployment https://hungreo-website-hojo1twmk-hungreos-projects.vercel.app -- --output /tmp/hungreo-ssr-release/candidate-0.html --write-out '%{http_code}'
vercel promote https://hungreo-website-hojo1twmk-hungreos-projects.vercel.app --yes
vercel alias ls
git diff --check
```

Public before/after requests dùng Python `urllib.request`, không dùng HEAD. Build Vercel hoàn thành cả compile/lint/typecheck. Build có warning dependency/auth Edge (`bcryptjs`, `jose`) và npm audit 21 vulnerabilities; không nâng dependency ngoài patch này. Anonymous auth smoke pass không thay thế authenticated admin UAT. Log sạch trong cửa sổ ngắn không phải cam kết không có lỗi về sau.

## Hưng test đơn giản khoảng 2 phút

1. Mở cửa sổ ẩn danh, vào thẳng [OpenClaw](https://hungreo.com/projects/openclaw-personal-ai-operations-platform) và [Bình An Trong Tâm Thức](https://hungreo.com/blog/awakening-inner-peace-lessons-from-thy-trn-vit-qun). Reload mỗi trang: có tiêu đề, nội dung, ảnh; không có trang lỗi.
2. Đổi EN/VI và bấm quay lại danh sách, mở lại bài: nội dung/điều hướng hoạt động bình thường.
3. Nếu kiểm tra kỹ hơn: F12 → Network → reload → request loại document đầu tiên phải là **200**, trước patch là **500**. Giao diện có thể trông giống trước vì JavaScript từng render lại dù document lỗi.

Gửi “PASS” nếu ổn; nếu lỗi gửi URL và mô tả/ảnh. Baseline trước deploy đã lưu nên không cần rollback để thử lại lỗi cũ. Chưa đánh dấu owner UAT PASS khi chưa nhận phản hồi.

Trước lần deploy từ Git tiếp theo cần đưa patch đã kiểm tra vào commit để tránh bản deploy Git làm mất fix; hiện chưa commit/push theo phạm vi lần này.
